{
    "name": "zz-double-install-test",
    "id": "zz-double-install-test",
    "version": "1.0.1",
    "description": "Test feature to check if a feature can be installed twice.",
    "options": {
      "execname": {
        "type": "string",
        "default": "latest",
        "description": "Name of the test executable to install."
      }
    }
  }